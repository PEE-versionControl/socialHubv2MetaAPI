rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // 匹配所有文件
    match /{document=**} {
      // 允許所有已通過 Firebase Authentication 驗證的使用者（包含匿名登入）進行讀寫
      // 這能解決上傳沒反應的問題，同時防止完全未經授權的訪問
      allow read, write: if request.auth != null;
    }
  }
}